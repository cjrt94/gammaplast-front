# Changelog — Gamma Plast

Formato: [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) + [Semantic Versioning](https://semver.org/)

---

## [Unreleased]

- fix: **back office — las fotos de "Procesos productivos" del Home ahora son editables** — estaban hardcodeadas en `index.vue` (array `stepImgs`) y no se podían cambiar desde el panel. Ahora cada paso de `home.process.steps` tiene campo `img`: el editor de Home suma un subidor **"Foto del proceso"** por paso, el Home lo lee (`s.img`, con fallback), y se rellenaron las 4 fotos actuales en Firestore. Verificado en navegador (front lee las 4 fotos del CMS; el panel muestra 4 subidores).
- fix: **back office — las ediciones se ven al instante (invalidación de cache)** — antes, una edición en `/admin` tardaba hasta 5 min en verse en la web por el cache SWR de las rutas `/api/content/*`. Ahora todo guardado/borrado (cableado en el punto común `useFirestoreCrud.set`/`remove`) llama a la nueva ruta autenticada `POST /api/admin/revalidate` que limpia el cache de contenido → el cambio se refleja de inmediato. Además se baja el TTL de 5 min a **60 s** (acota la latencia en Vercel, donde las funciones son efímeras/multi-instancia y la invalidación es best-effort por instancia). Verificado end-to-end: sin invalidar el front queda stale; al invalidar se actualiza al instante (9 entradas de contenido limpiadas).
- refactor: **back office (Fase 6 — limpieza)** — se elimina el contenido hardcodeado ya migrado a Firestore: se borra `composables/useCatalog.js` (sin uso), `data/novedades.js` queda solo con `formatDate`, y se quitan de `i18n/locales/{es,en}.json` los bloques `home`, `seo`, `faq` y `categories` (ahora en el CMS). Se conserva el chrome de i18n (`topbar`, `nav`, `cta`, `footer`). Build OK; las 7 páginas públicas renderizan 200.
- feat: **back office (Fase 5.2 — el sitio público lee TODO desde Firestore)** — se cierra el loop editar→ver: las ediciones del panel ya se reflejan en la web. Swaps con `await useFetch`/`useAsyncData` (SSR espera los datos, JSON-LD correcto): **Contacto** como única fuente de verdad vía `useContact()` — mata la duplicación x4 (`app.vue` JSON-LD, `AppFooter`, `AppTopBar`, `contacto.vue` y `contacta-un-asesor.vue` leen dirección/teléfonos/correos/mapa del singleton); **Home** (`index.vue`: hero, banda de certificaciones, planta, procesos y encabezado de productos desde `/api/content/home` + sectores desde `/api/content/catalog`); **Productos** (`productos/index.vue`: catálogo + materiales + FAQ desde el CMS); **Nosotros** (`nosotros.vue`: hero, valores, misión/visión, procesos, galería y grid de certificaciones); **SEO por página** vía nuevo `usePageSeo()` (títulos/descripciones y FAQ desde `singletons/seo`) en home/nosotros/productos/novedades/contacto/asesor/trabaja. **Verificado en SSR** contra el Firestore real: home/nosotros/productos/contacto renderizan el contenido del CMS (hero, valores, certificaciones, FAQ, dirección, teléfono en footer) + build OK.
- feat: **back office (Fase 4 — editores CRUD completos + bandeja de leads)** — el panel `/admin` ya permite gestionar **toda la información** del sitio. **Editores de singletons**: Home, Nosotros (hero/valores/misión-visión/procesos/galería/certs), Materiales, Contacto (única fuente de verdad) y SEO+FAQ. **Colecciones con lista + alta/edición**: Sectores, Productos (filtro por sector, `id=slugify(name)` con guardia de colisión), Novedades (cuerpo de párrafos, borrador/publicada) y Certificaciones; **gestores de logos** de clientes y de certificación (editar/agregar/reordenar en línea). **Bandeja de leads**: Mensajes de contacto, Solicitudes de asesor y Postulaciones (marcar leído/no leído, eliminar, **descarga de CV** vía URL firmada temporal desde ruta autenticada `POST /api/admin/cv-url` que verifica el ID token). Componentes reutilizables (`components/admin/`): `AdminList`, `ImageUploader` (sube a Storage `content/<carpeta>/`), `RepeatableList`, `StringList`, `ConfirmDialog` (**sin `confirm()` nativo**), `SaveBar`, `LeadInbox`, `LogoManager`; composables `useFirestoreCrud`/`useSingleton`, `useConfirm`, `useSaveState`, `useLeads`. **Verificado end-to-end** contra el Firestore real: las 20 rutas de `/admin` renderizan 200; el flujo real de escritura (login staff → editar singleton → revertir; alta+borrado de producto) funciona bajo las security rules; la ruta del CV rechaza sin token (401), devuelve URL firmada con token y la descarga funciona; build OK.
- feat: **back office (Fase 2 — auth + shell del panel `/admin`)** — login de staff con Firebase Auth (email/password) y esqueleto del back office: `composables/useAdminAuth.js` (sesión en `useState`, `onAuthStateChanged`, `login`/`logout`; `firebase/auth` dinámico, fuera del SSR), `middleware/admin.global.js` (protege `/admin/**` salvo el login; gate en cliente porque el panel no es SEO), `layouts/admin.vue` (sidebar con grupos Contenido/Bandeja + barra con usuario y cerrar sesión), `pages/admin/login.vue` (form con errores amables, sin `alert`) y `pages/admin/index.vue` (dashboard con contadores de leads sin leer + accesos a contenido). **Security rules desplegadas** (`firebase deploy --only firestore:rules,storage`). **Verificado**: login end-to-end por Web SDK, reglas (anon lee contenido / bloqueado en leads; staff lee leads y escribe contenido), páginas `/admin` y `/admin/login` renderizan 200, home pública intacta, build OK. Usuario staff inicial creado (`admin@gammaplast.com.pe`). Pendiente: los editores CRUD (Fase 4) — los enlaces del panel a Home/Nosotros/Productos/etc. aún no existen.
- feat: **back office (Fase 5.1 — lectura pública desde Firestore: infra + primeros swaps)** — el sitio empieza a leer contenido del CMS. Nuevas **rutas Nitro cacheadas** (`server/api/content/*.get.js`, `defineCachedEventHandler` SWR 5 min, lectura por Admin SDK ⇒ ignora security rules y mantiene el Web SDK fuera del bundle público): `catalog`, `home`, `nosotros`, `contact`, `seo`, `clientLogos`, `novedades` (lista) y `novedades/[slug]` (404 si no existe); helper `server/utils/content.js` (ordena por `order`, limpia Timestamps). **Swaps hechos** (mismo render, sin cambio visible): `ClientLogos.vue` (marquee), **Novedades** lista + detalle (`pages/novedades/*`, con top-level `await useFetch` para que el SSR/JSON-LD espere los datos) y el **sitemap** (`server/api/__sitemap__/urls.js` lee novedades de Firestore). **Verificado en SSR** (HTML server-rendered con los datos reales: logos en home, 3 títulos en novedades, detalle 200 + h1, 404 de slug inexistente, sitemap con 3 URLs) + build OK. Pendiente de swap: contact (dup x4), catálogo, nosotros, home/SEO.
- feat: **back office (Fase 3 — persistencia de leads)** — los 3 endpoints (`contacto`/`asesor`/`postular`) ahora **guardan cada envío en Firestore** además del email, para la bandeja de `/admin`: `contactMessages`, `advisorRequests`, `jobApplications` (con el CV subido a Storage privado `cv/<año>/<uuid>-<archivo>` y `cvPath`/`cvFilename` en el doc). Cada lead se persiste tras la validación, **independiente del email** (el registro es la fuente durable — hoy se pierden) y en `try/catch` → un fallo de Firestore/Storage nunca rompe el submit; el email sigue siendo el canal primario. `server/utils/leads.js` (`saveLead`, `uploadCv`). Todos los leads nacen con `read:false` + `createdAt` de servidor. **Verificado end-to-end** contra el Firestore real (3 docs escritos con sus campos + CV en Storage; datos de prueba limpiados).
- feat: **back office (Fase 0 — infra Firebase, sin cambio de comportamiento)** — se añaden las bases del panel `/admin` sobre Firebase: init del **Web SDK** cliente (`plugins/firebase.client.js`, `.client` ⇒ fuera del SSR, provee `$fbAuth/$fbDb/$fbStorage`, no-op sin config) e init singleton del **Admin SDK** de servidor (`server/utils/firebaseAdmin.js`, service account en base64 vía env → `adminDb()`/`adminBucket()`). `runtimeConfig` extendido (`firebaseServiceAccount`, `firebaseStorageBucket`, `public.firebase.*`) + `.env.example` documentado; `firestore.rules` + `storage.rules` + `firebase.json` (contenido lectura pública/escritura staff; leads escritura solo servidor y lectura solo staff; CVs privados); deps `firebase@11` + `firebase-admin@13`; `serviceAccount*.json` en `.gitignore`. Nada consume Firebase aún → sitio idéntico. **Build OK.**
- feat: **back office (Fase 1 — script de seed)** — `scripts/seed.mjs` (`npm run seed`) migra TODO el contenido hardcodeado a Firestore (7 sectores, 38 productos con `id=slugify(name)` + guardia de colisión, 3 novedades, 6 certificaciones, 7 logos de certificación, 13 logos de clientes, y singletons `materials/home/nosotros/contact/seo`). Idempotente (upserts por id, `--dry-run`), imágenes sembradas como rutas públicas actuales. `contact` queda como **única fuente de verdad** (hoy duplicado en app.vue/footer/topbar/páginas). `--dry-run` verificado. Pendiente: crear el proyecto Firebase + credenciales para ejecutarlo.
- feat: **carousel infinito de logos de clientes bajo el hero del Home** (`ClientLogos.vue`) — marquee CSS puro (sin JS), duplica la lista y desplaza -50% para un loop sin salto; logos en escala de grises (a color al hover), máscara de degradado en los bordes, pausa al hover y degradación a grilla estática con `prefers-reduced-motion`. **Logos PLACEHOLDER** de marcas reconocidas de alimentos (Nestlé, PepsiCo, Coca-Cola, Danone, Mondelēz, Kraft Heinz, Unilever, General Mills, Mars, Kellogg's, Bimbo, Alicorp, Gloria) en `public/logos/clientes/` — pendiente reemplazar por los clientes reales. Verificado en navegador (26 imgs cargan OK, build OK)
- fix: **chip de filtro de "Limpieza" muestra el nombre completo** en Productos — el sector `limpieza-y-cuidado-personal` usaba el `short` "Limpieza"; ahora el chip lee **"Limpieza y cuidado personal"** como pidió el cliente
- chore: **quitar la sección "Nuestra planta" bajo el hero de Nosotros** a pedido del cliente — se elimina el bloque "Producción integrada en nuestra planta" (galería duplicada del Home); la página va del hero directo a Valores. La galería de planta al pie ("Detrás de cada empaque") se mantiene
- feat: **íconos oficiales del cliente en "Nuestros valores"** (Nosotros) — se reemplazan los SVG aproximados de `BaseIcon` por los **6 íconos del set oficial** que envió el cliente (foco/órbita, bolsa, planeta+hojas, reciclaje, rollo, empaque), extraídos de su lámina y recoloreados al verde de marca con fondo transparente (`public/photos/valores/*.png`). Verificado en navegador
- fix: **ubicación de "Contacta un asesor" en una sola línea + estructura peruana** — los 3 campos de ubicación pasan a **Departamento · Provincia · Distrito** en una sola fila (`grid-cols-3` siempre, `legend` en su propia línea); antes eran País/Ciudad/Distrito y se apilaban/desalineaban. Renombre de claves consistente en el form, en `advisorSchema` (Zod) y en el correo del endpoint `/api/asesor`. Verificado (build + 422 + render desktop/móvil)
- chore: **quitar la franja verde al pie del hero** a pedido del cliente
- chore: **video del hero invertido horizontalmente** (`hflip`) a pedido del cliente — se reencodea `public/videos/hero.mp4` y su póster desde el `.mov` con `hflip`; el rollo de film queda a la derecha y el texto sobre la zona oscura izquierda
- feat: **hero del Home full-bleed con video de fondo** (como pidió el cliente, referencia de la primera web) — el hero pasa del split (texto + collage de productos sobre fondo claro) a **medios a sangre**: `<video>` mudo en loop con póster de fallback (`public/videos/hero.mp4` + `hero-poster.jpg`), velo oscuro en degradado para contraste AA del texto blanco, titular blanco con acento verde brillante, lead y **CTA "Contacta a un asesor"** (se mantiene el flujo del 17-jul), botón secundario claro y franja de marca al pie. El **header va transparente sobre el hero en Home** (logo/nav en blanco) y pasa a sólido al hacer scroll o abrir el menú; la **barra superior se oculta en Home**. Verificado en navegador (desktop + móvil, sin overflow) y transición overlay→sólido; `npm run build` OK
- feat: **reestructura de Productos por los 7 sectores del catálogo** (feedback `web.docx`) — `useCatalog` pasa de "familias por tipo + filtro de industria" a **producto etiquetado por sector** (Alimentos y bebidas · Pesca y agroindustria · Textil · Envíos seguros y valores · Limpieza y cuidado personal · Retail y marketing · Industria en general) con los 37 productos del documento; filtro por sector; tarjetas **más verticales (4:5, `object-contain`)** con el producto completo y **sin franja-banner** (encabezado de texto + divisor por sector); bloque **"Estructuras y materiales"** (Monocapa/Laminado + paleta, catálogo p8) junto al CTA. Las tarjetas de categoría del Home pasan a los 7 sectores. Verificado en navegador
- feat: **Contacto en dos páginas** (como pide el cliente) — `/contacto` (contacto general, form corto) y **`/contacta-un-asesor`** (lead calificado: "Hablemos de tu próximo empaque" + Empresa/RUC/Ubicación país-ciudad-distrito, todo obligatorio); router: botón verde del header y CTAs de Home/Nosotros/Productos → asesor, enlace "Contacto" del menú → general, footer con ambas; endpoints `POST /api/asesor` y `POST /api/contacto` con `advisorSchema`/`contactSchema`. Verificado (422/500 + render)
- feat: **feedback del Home** — **tira de logos de certificación** (recortados del catálogo p5 con fondo transparente por flood-fill); **fotos por proceso** en las 4 tarjetas (2 por fila, más altas); secciones **separadas por fondo alternado** (procesos gris ↔ nuestra planta blanco ↔ productos gris); las 7 tarjetas de sector llenan el ancho (4+3); sin "Solicitar presupuesto"
- feat: **feedback de Nosotros** — texto e intro del catálogo; **6 valores** con sus símbolos (foco/bolsa/planeta/reciclaje/rollo/empaque); **fotos por proceso** en el timeline; sección "Producción integrada en nuestra planta"; sin botones "Contáctanos" ni "Cotiza tu empaque"
- feat: **assets del banco/catálogo + iconos** — 4 fotos de proceso, 7 escenas de sector, 3 spreads de marca y ~15 recortes de producto (`public/photos`/`public/products`); 7 logos de certificación individuales en `public/photos/certs/`; iconos nuevos en `BaseIcon` (shirt/package/spray/bag/roll). Footer alineado al header (sin "Trabaja con nosotros", con "Contacta un asesor")
- fix: **heroes de página alineados a la izquierda** — `PageHero` dejaba una caja centrada con el texto a la izquierda (no coincidía con el contenido de abajo); ahora el bloque se ancla al borde izquierdo del wrap
- chore: **config de deploy Vercel** — `vercel.json` (framework nuxtjs), `.vercelignore` (excluye brand/design/docs) e ignorar `.vercel`
- docs: **plan de feedback del cliente** (`docs/PLAN-FEEDBACK-CLIENTE.md`) y **mapeo del banco de imágenes** (`docs/MAPEO-IMAGENES.md`)
- fix: **la navegación entre páginas dejaba la página nueva en blanco** — dos causas encadenadas introducidas al añadir la page transition `out-in` (Fase 2): (1) **ninguna página tenía un nodo raíz único** (empezaban con comentario o `<PageHero>` + `<section>` hermanos = fragment), y `<Transition>` no puede animar un multi-root → al navegar en cliente la página entrante no renderizaba (Nuxt lo avisaba: *"does not have a single root node…"*). Se envuelve el template de las 7 páginas en un `<div>` raíz. (2) El **reveal on scroll** se reenganchaba con un `setTimeout(…, 80 ms)` tras el cambio de ruta, pero con `mode: out-in` la página entrante monta a **~250 ms** (tras la salida) → el reveal corría antes de existir el DOM nuevo y dejaba el contenido sobre el fold en `opacity:0`. Ahora se engancha al hook **`page:transition:finish`** de Nuxt. Verificado en navegador: navegación multi-salto (Home↔Nosotros↔Productos↔Novedades↔Contacto) con `h1` y contenido visibles; `npm run build` OK
- refactor: **componentización de la galería** — el bloque figura+imagen con zoom, duplicado **13×**, se extrae a **`<ZoomImage>`** (imagen `object-cover` con zoom sutil al hover) y **`<GalleryFigure>`** (celda de mosaico: `figure` + `reveal` + `ZoomImage`); usados en Home, Nosotros y las cards de Novedades. Centraliza radio/ratio/zoom/`loading`/dimensiones en un solo lugar. Sin cambios visuales (verificado en navegador)
- feat: **quick wins de UX/a11y + detalle de motion** — (a11y) el accent del H1 del Home pasa de `text-green` a **`green-700`** para cumplir **AA** sobre blanco (2.3→5.2:1); (motion) filtro de Productos con **FLIP** (`<TransitionGroup>`) + **estado vacío**, quitando `.reveal` de las cards para eliminar la colisión reveal↔filtro; el **reveal on scroll se vuelve idempotente** (WeakSet) → un cambio de ruta o de filtro ya no re-oculta lo visible ni duplica triggers; y el **timeline de "Procesos productivos"** (Nosotros) dibuja su conector con **CSS scroll-driven** (`animation-timeline: view()`, degrada a línea estática). Verificado en navegador (H1 green-700, filtro sin flash de cabecera, cards siempre visibles). Nota: las imágenes de Novedades ya reservaban espacio (`aspect-[16/10]` + `width/height`) — sin CLS
- feat: **animaciones — Fase 2 (micro-interacciones + transiciones)** — **page transitions** entre rutas (cross-fade con micro-rise, `out-in` de Nuxt → cierra el FOUC de navegación cliente); fix del **`.btn-ghost`** que cambiaba color de golpe (ahora transiciona); **header sticky "scrolled"** (compacta 72→58px + borde inferior al despegar del top, sin sombra por regla de marca); **acordeón FAQ** animado con `::details-content`/`interpolate-size` (degrada a toggle instantáneo donde no hay soporte); **transición éxito↔formulario** en Contacto y Trabaja con nosotros (`<Transition rise>` out-in); el reveal se acota a `#main .reveal` para no re-ocultar el chrome persistente (footer/header) en cada navegación. Verificado en navegador (header 72→58, navegación, acordeón)
- feat: **sistema de animaciones — Fase 1 (motor GSAP)** — **tokens de motion** (`--dur-*`/`--ease-*`/`--rise`/`--lift`/`--stag` en `:root` + espejo en `tailwind.config.js`); el reveal on scroll migra de `IntersectionObserver` a **GSAP `ScrollTrigger.batch`** (escalona lo que entra junto al viewport, `gsap.matchMedia` para reduced-motion), con GSAP en **import dinámico client-only** (chunk lazy, fuera del critical path). **P0 de robustez**: (1) reveal con *progressive enhancement* — visible sin JS (la clase `html.js` gobierna el ocultamiento **antes del paint**, sin FOUC); (2) imagen **LCP** del hero (doypack) desacoplada del reveal para no retrasar el LCP; (3) **`prefers-reduced-motion` global** — neutraliza también los hover-lift y los 13 zoom `group-hover:scale`, conservando el feedback de color. Verificado en navegador (reveal 0→1 al hacer scroll, LCP visible al instante, reduced-motion muestra todo)
- feat: **backend de formularios con Resend** — endpoints Nitro `POST /api/contacto` (→ comercial@/comercial2@) y `POST /api/postular` (→ recursoshumanos@, con el **CV adjunto**); revalidación server-side con el **mismo schema Zod** del cliente (`composables/leadForm.js`), **honeypot** anti-spam, y validación de CV (PDF/DOC/DOCX, ≤ 5 MB); `runtimeConfig` privado (env `NUXT_RESEND_API_KEY`/`NUXT_MAIL_FROM`/destinatarios) + `.env.example`. Los formularios ahora hacen `$fetch` real (JSON en contacto, `FormData` en postular). Verificado: honeypot→200, inválido→422, válido→guard de config (falta la API key para el envío real). De paso, los campos opcionales del schema aceptan clave ausente (`.default('')`)
- refactor: **validación de formularios con el patrón del equipo (Zod)** en Contacto y Trabaja con nosotros — se reemplaza el `validate()` casero por schemas Zod compartidos (`composables/leadForm.js`: `contactSchema`/`careersSchema`) con `.min(1)` obligatorio + `.max(N)` por campo, enlazados a `maxlength` en cada input (nombre/apellido 40, teléfono 20, correo 120, empresa 80, mensaje 1500); el envío usa `schema.parse()` para normalizar (trim/lowercase) y el mismo schema queda listo para revalidar server-side (TODO Dean). Elimina la duplicación de reglas entre ambos forms
- fix: el **hover-lift de las cards** (`.card-hover`) no se aplicaba en cards con `.reveal` — la animación de entrada fijaba `transform` en reposo (`transform: none`, unlayered) y pisaba el `translateY` del hover; `.reveal` pasa a `@keyframes` con `fill: backwards`, así el hover-lift vuelve a funcionar en Home (Productos por industria), Novedades y Productos; el stagger usa `animationDelay`
- feat: capa de **SEO para buscadores y agentes de IA** — `hreflang`/`canonical`/`<html lang>` por locale vía `useLocaleHead` (corrige el `lang` fijo en `/en`); meta por página (`useSeo` + namespace `seo.*` en i18n ES/EN); **sitemap i18n** (`@nuxtjs/sitemap`, 18 URLs incl. las notas dinámicas); **JSON-LD** — grafo global `Organization`+`LocalBusiness`+`WebSite` y por página `CollectionPage`+`ItemList` (33 productos), `NewsArticle`, `AboutPage`/`ContactPage`/`WebPage` y `BreadcrumbList`; `robots.txt` con allow explícito a crawlers de IA (GPTBot/ClaudeBot/PerplexityBot/Google-Extended…); `llms.txt`; sección **FAQ + `FAQPage`** en Productos; favicon + `og-cover.jpg`. Verificado en dev (head, hreflang, 13 rutas 200) y build (sitemap con dominio real)
- docs: informe de **revisión QA + UX + UX writing** (`docs/REVISION-QA-UX.md`)
- refactor: **reestructura del catálogo de Productos por tipo de empaque** con filtro por industria — elimina el drilldown `/productos/[categoria]`; `useCatalog` aplanado (33 productos, 5 familias, 4 industrias)
- feat: **fotos reales** (banco Pexels) — hero + galería "Detrás de cada empaque" en Nosotros y sección "Nuestra planta" (mosaico editorial) en Home; `public/photos/` + créditos
- refactor: **rediseño editorial del detalle de Novedades** (contenedores centrados, lead, imagen destacada, "Más novedades" con imágenes) + imágenes en el listado
- style: Home — separación visual hero/siguiente sección; copy — título "Todo bajo un mismo techo"→"Producción integrada en nuestra planta", valor "Damos siempre más"→"Compromiso con cada cliente", CTA "Ver catálogo completo…"→"Ver todos los productos"
- feat: rediseño de la página **Nosotros** alineado al diseño de Figma — hero editorial dividido (copy oficial), valores del Figma, Misión/Visión con acento verde, **Nuestros Procesos Productivos** como timeline numerado (copy completo del diseño) y Certificaciones en banda verde `green-700`. Validado con Impeccable (AA, audit 18/20)
- fix: corregir el tamaño del logo en nav y footer — el logo horizontal venía en lienzo cuadrado 2000² con ~78% de margen (se veía diminuto); se recorta al contenido (`public/logo-h.png` y `logo-white-h.png`, ratio ~2.94:1) y se muestra a 40/44px con `width/height` intrínsecos correctos
- fix: pasada de accesibilidad y diseño en todo el sitio (Impeccable) — contraste AA (botón sólido `.btn-green`→`green-700`, blanco 2.85→5.19:1; foco visible en inputs, WCAG 2.4.7; `slate` 4.44→4.77:1 sobre mist), skip-link, `aria-label` del selector de idioma, touch targets ≥44px, `width/height` en imágenes (CLS); fixes por página: Home (card de sector destacada de gradiente a acento, category-cards enlazan a su sector, grid sin huérfana), Productos (imagen única por producto, hero con PageHero, hover falso removido), Novedades (`datetime`, placeholders→copy, escala tipográfica, aria-label), formularios (input de CV accesible por teclado, estados loading/error/éxito accesibles, validación client-side)
- chore: inicializar Impeccable — `PRODUCT.md` (register=brand, personalidad, anti-referencias, principios, AA) y `DESIGN.md` (tokens reales verificados) como contexto de diseño para los comandos
- docs: documento de estado/handoff (`docs/ESTADO.md`) — cómo correr, estructura, estado por página, backlog priorizado (bloqueantes/contenido/mejoras), decisiones abiertas y notas de deuda técnica, para retomar el desarrollo
- feat: scaffold del proyecto **Nuxt 3** (JS nativo) + Tailwind + i18n ES/EN — sistema de diseño en `tailwind.config.js`/`assets/css/main.css` (tokens, Corbel embebida, pills/botones/cards), layout con top bar + header (nav, selector de idioma, CTA) + footer sin CTA, componentes base (`BaseIcon`, `SectionHeader`) y mensajes i18n
- feat: página **Home** portada del key visual aprobado — hero, procesos productivos (sin fotos, 01–04), productos por 4 categorías (Alimentos y bebidas destacado, sin detalle) y stubs de las demás rutas. Verificada corriendo en Nuxt dev (HTTP 200)
- feat: **Nosotros** — intro oficial, valores (Excelencia/Trabajo en equipo/Innovación/Respeto/Integridad), misión y visión, capacidades productivas (01–04 con detalle de materiales/procesos) y certificaciones (RCS/TÜV/D2W/D2P/ISO/FDA)
- feat: **Productos** — índice de 4 categorías + plantilla dinámica `/productos/[categoria]` (breadcrumb, descripción oficial y productos de referencia con imágenes dummy); catálogo en `composables/useCatalog.js`; 33 dummies en `public/products/`
- feat: **Contacto** — formulario (Nombre/Apellido/Teléfono/Correo/Empresa/Mensaje) con estado de éxito inline, datos reales (comercial@/comercial2@) y mapa embebido de Ate, Lima. **Trabaja con nosotros** — formulario con carga de CV → recursoshumanos@. *(Envío por correo pendiente de backend.)*
- feat: **Novedades** — listado + plantilla de artículo `/novedades/[slug]` con data estática (`data/novedades.js`, incluye la nota real del mockup). *(Migración a @nuxt/content pendiente.)*
- feat: componentes `PageHero`, `SectionHeader`, `BaseIcon` (set ampliado); SEO base (JSON-LD `LocalBusiness`, OG, `robots.txt`). Las 13 rutas verificadas (200; categoría inexistente → 404). Sitio bilingüe ES/EN (EN cae a ES hasta traducir)
- docs: plan de rediseño completo (`docs/PLAN-REDISENO.md`) basado en el brief oficial `Info.docx` (+ catálogo como referencia visual) — arquitectura de páginas (Home, Nosotros, Productos + 4 categorías, Novedades, Contacto, Trabaja con nosotros), sitio bilingüe ES/EN, contenido por página, stack, roadmap por fases y decisiones abiertas; + sistema de diseño bloqueado (`docs/DESIGN.md`) con tokens, tipografía, radios/sombras y componentes derivados del key visual
- design: key visual del Home aprobado (`design/home.html`) — dirección corporativa institucional (tema claro, sin sombras, radios reducidos, pills outline verde); hero + procesos productivos (sin fotos) + productos por sector (sin detalle) + footer sin CTA
- chore: inicialización del proyecto — brand kit importado (`brand/`), guía de marca en `docs/BRAND.md` (paleta, tipografía, logotipo) y registro en el sistema de Sam como web corporativa (Nuxt 3 + Tailwind + Vercel)
